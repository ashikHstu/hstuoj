let ok=0;
let page=document.body.id;


switch(page)
{
    case 'signinok':
     ok=1;
     prompt("Now at signinok With ok = "+ok+" : ");
    break;
    case 'signin':
     
        prompt("Now at signin With ok = "+ok+" : ");
    break;

    case 'problemset':

        prompt("Now at Problemset With ok = "+ok+" : ");

    break;
    case 'index':
        prompt("Now at index With ok = "+ok+" : ");

    break;


    case 'aboutus':


    break;


    case 'contact':

    break;

    case 'signup':
        let x=prompt("Now at signup With ok = "+ok+" : ");
    break;

    default:

}